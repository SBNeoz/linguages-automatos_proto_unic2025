class Dancarina {
    public static void main(String[] args) throws InterruptedException {
        System.out.println("🎵 Iniciando a dança!");
        for (int i = 0; i < 2; i++) {
            girar();
            mover(10);
            mudarCor();
            Thread.sleep(1000);
        }
        System.out.println("💃 Dança concluída!");
    }

    static void girar() {
        System.out.println("A dançarina gira 360°");
    }

    static void mover(int passos) {
        System.out.println("A dançarina se move " + passos + " passos");
    }

    static void mudarCor() {
        System.out.println("A dançarina muda a cor do traje");
    }
}

echo "# linguages-automatos_proto_unic2025" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/SBNeoz/linguages-automatos_proto_unic2025.git
git push -u origin main
